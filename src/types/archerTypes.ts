export type ArcherType = {
  clubId: string | null;
  club: { name: string } | null;
  firstName: string;
  lastName: string;
  email: string | null;
  username: string;
  id: string;
  createdAt: string;
  updatedAt: string;
  public: boolean;
};
